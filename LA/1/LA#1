class hero:
    name = "layla"
    
mm = hero()
print(mm.name)

#LA-2

class hero: 
    def __init__(self, name, role):
        self.name = name
        self.role = role
        
character = hero("layla", "marksman")
print(character.name, character.role)
